<UserControl
    d:DesignHeight="700"
    d:DesignWidth="1000"
    mc:Ignorable="d"
    x:Class="Store.Views.HistoryPageView"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border CornerRadius="16" Background="White" Margin="10" BoxShadow="0 4 20 0 #30000000">
        <Grid RowDefinitions="Auto,Auto,*">
            <!-- Header & Controls -->
            <Border Grid.Row="0" Background="#F7FAFC" CornerRadius="16,16,0,0" Padding="20,20,20,15">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Lịch sử nhập kho" Classes="header" Margin="0,0,0,5"/>
                        <TextBlock Text="Theo dõi các đợt nhập hàng vào kho" Classes="body"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" 
                            Classes="primary"
                            Height="42"
                            Padding="24,0"
                            VerticalAlignment="Center">
                        + Nhập kho mới
                    </Button>
                </Grid>
            </Border>

            <!-- Search & Filter -->
            <Border Grid.Row="1" Padding="20,15" BorderBrush="#E2E8F0" BorderThickness="0,1,0,1">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Border CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1.5">
                        <ComboBox Background="White"
                                  BorderThickness="0"
                                  Width="180" 
                                  FontSize="14"
                                  PlaceholderText="Lọc theo tháng"
                                  Padding="12,8"
                                  CornerRadius="8"/>
                    </Border>
                    
                    <Border CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1.5">
                        <TextBox Background="White"
                                 BorderThickness="0"
                                 Width="400" 
                                 FontSize="14"
                                 Watermark="🔍 Tìm kiếm phiếu nhập..."
                                 Padding="12,8"
                                 CornerRadius="8"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- History List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel Spacing="12">
                    <!-- History Item Template -->
                    <Border Classes="card" BoxShadow="0 2 8 0 #20000000">
                        <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                            <!-- Icon -->
                            <Border Grid.Column="0" 
                                    Background="#F7FAFC" 
                                    CornerRadius="10" 
                                    Width="50" 
                                    Height="50"
                                    VerticalAlignment="Center"
                                    Margin="0,0,15,0">
                                <TextBlock Text="📦" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <!-- Info -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Phiếu nhập #NK001" FontSize="15" FontWeight="SemiBold" Foreground="#2D3748"/>
                                <TextBlock Text="Ngày nhập: 01/01/2024" FontSize="13" Foreground="#718096" Margin="0,5,0,0"/>
                            </StackPanel>

                            <!-- Amount -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0">
                                <TextBlock Text="Tổng giá trị" FontSize="12" Foreground="#718096" TextAlignment="Right"/>
                                <TextBlock FontSize="16" FontWeight="Bold" Foreground="#667EEA" TextAlignment="Right" Margin="0,5,0,0">
                                    <Run Text="0"/>
                                    <Run Text=" ₫" FontSize="13"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Action -->
                            <Button Grid.Column="3" 
                                    Classes="secondary" 
                                    Padding="12,8"
                                    VerticalAlignment="Center">
                                Chi tiết
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Empty State -->
                    <Border Background="#F7FAFC" 
                            CornerRadius="12" 
                            Padding="40"
                            Margin="0,20,0,0">
                        <StackPanel HorizontalAlignment="Center" Spacing="15">
                            <TextBlock Text="📦" FontSize="64" HorizontalAlignment="Center"/>
                            <TextBlock Text="Chưa có lịch sử nhập kho" 
                                       FontSize="18" 
                                       FontWeight="SemiBold"
                                       Foreground="#2D3748"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Tạo phiếu nhập kho đầu tiên để bắt đầu" 
                                       FontSize="14" 
                                       Foreground="#718096"
                                       HorizontalAlignment="Center"/>
                            <Button Classes="primary" Margin="0,10,0,0">+ Nhập kho mới</Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
