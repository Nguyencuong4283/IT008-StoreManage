<Window
    Background="Transparent"
    Height="800"
    SystemDecorations="None"
    Title="Create Product"
    Width="650"
    WindowStartupLocation="CenterScreen"
    d:DesignHeight="800"
    d:DesignWidth="650"
    mc:Ignorable="d"
    x:Class="Store.CreateCustomerWindowView"
    x:DataType="vm:CreateCustomerWindowViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Store.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border
        Background="{DynamicResource BgPage}"
        BorderBrush="{DynamicResource BorderColor}"
        BorderThickness="2"
        BoxShadow="0 10 40 5 #40000000"
        CornerRadius="20">

        <Grid>
            <Button
                Classes="close-btn"
                Command="{Binding $parent[Window].Close}"
                HorizontalAlignment="Right"
                IsTabStop="False"
                Margin="0,5,10,0"
                VerticalAlignment="Top"
                ZIndex="10">
                ✕
            </Button>

            <Border
                Background="{DynamicResource BgCard}"
                BoxShadow="0 8 32 0 #20000000"
                CornerRadius="16"
                Margin="60,70,60,60"
                Padding="50,40">
                <StackPanel>
                    <TextBlock
                        FontSize="28"
                        FontWeight="Bold"
                        Foreground="{DynamicResource TextPrimary}"
                        Margin="0,0,0,40"
                        TextAlignment="Center">
                        Tạo khách hàng mới
                    </TextBlock>

                    <Grid ColumnDefinitions="*,*" Margin="0,0,0,20">
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Classes="label" Text="Mã khách hàng" />
                            <TextBox
                                Classes="modern-input"
                                Foreground="{DynamicResource PrimaryText}"
                                IsReadOnly="True"
                                IsTabStop="False"
                                Text="{Binding MaKH}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Classes="label" Text="Tên khách hàng" />
                            <TextBox
                                Classes="modern-input"
                                Text="{Binding TenKH, Mode=TwoWay}"
                                Watermark="Nguyễn Văn A">
                                <Interaction.Behaviors>
                                    <FocusOnAttachedBehavior />
                                </Interaction.Behaviors>
                            </TextBox>
                        </StackPanel>
                    </Grid>

                    <Grid ColumnDefinitions="*,*" Margin="0,0,0,20">
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Classes="label" Text="Số điện thoại" />
                            <TextBox
                                Classes="modern-input"
                                Text="{Binding SDT, Mode=TwoWay}"
                                Watermark="+84 xxx xxx xxx" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Classes="label" Text="Giới tính" />
                            <ComboBox
                                Classes="modern-combo"
                                HorizontalAlignment="Stretch"
                                ItemsSource="{Binding DanhSachGioiTinh, Mode=OneWay}"
                                PlaceholderText="Chọn giới tính"
                                SelectedItem="{Binding GioiTinh}" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Classes="label" Text="Địa chỉ" />
                        <TextBox
                            AcceptsReturn="True"
                            Classes="modern-input"
                            Height="70"
                            Text="{Binding DiaChi, Mode=TwoWay}"
                            TextWrapping="Wrap"
                            VerticalContentAlignment="Top"
                            Watermark="Nhập địa chỉ đầy đủ..." />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Classes="label" Text="Ghi chú" />
                        <TextBox
                            AcceptsReturn="True"
                            Classes="modern-input"
                            Height="100"
                            Text="{Binding GhiChu, Mode=TwoWay}"
                            TextWrapping="Wrap"
                            VerticalContentAlignment="Top"
                            Watermark="Thêm ghi chú (tùy chọn)..." />
                    </StackPanel>

                    <Button
                        Classes="primary"
                        Command="{Binding CreateCusomterButtonCommand}"
                        FontSize="16"
                        Height="52"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center">
                        ✓ Tạo khách hàng
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>

</Window>